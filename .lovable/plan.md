## План изменений

### 1. Скрыть блок «Соловьёв LIVE / СОЛОВЬЁВ КВИЗ» на внутренних страницах
Сейчас хиро-блок (логотип `logo_main.svg` + крупный заголовок «Соловьёв Квиз») есть на:
- `src/pages/Auth.tsx` — экран входа (стартовый, оставляем как есть).
- `src/pages/Index.tsx` (строки 55–63) — главная со списком квизов.

Убираем хиро-блок (логотип + заголовок «Соловьёв Квиз» + подзаголовок-италик) из `src/pages/Index.tsx`. Список квизов, поиск и категории остаются.

### 2. Переименование квиза
Миграция:
```
UPDATE quizzes
SET title = 'Вторая мировая: правда или миф'
WHERE id = '40d7f10f-731d-450d-bfe2-88c947859ffd';
```

### 3. Обновить пояснение к вопросу про Ленд-лиз
Тот же вопрос `id = f9288037-…`, новый `explanation`:

> СССР получал помощь от многих стран: Великобритании (боевые самолёты, танки, боеприпасы), Китая, Ирана, Монголии, а также от внутренних ресурсов страны — собственная промышленность производила миллионы танков, самолётов и орудий.

### Технические детали
- Пункт 1 — правка только `src/pages/Index.tsx`.
- Пункты 2 и 3 — одна миграция с двумя UPDATE.
