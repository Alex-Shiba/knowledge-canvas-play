## Изменения на финальном экране квиза (`src/pages/QuizPlay.tsx`)

### 1. Убрать кнопки внизу
Удалить блок с кнопками "К квизам" и "Пройти снова" (`<RotateCcw />`).

### 2. Добавить сообщение про розыгрыш
После списка результатов добавить блок:
- Если пользователь ответил правильно на ВСЕ вопросы (`score === questions.length`) — показать поздравление: «Поздравляем! Вы ответили правильно на все вопросы и попадаете в розыгрыш призов. Победитель будет выбран случайным образом среди всех участников, прошедших квиз без ошибок.»
- Иначе — нейтральное сообщение: «Спасибо за участие! В розыгрыше призов участвуют только те, кто ответил правильно на все вопросы.»

Стилизованный блок (карточка с иконкой подарка `Gift` из lucide-react, акцентная рамка).

### 3. Зелёная галочка для правильных ответов
В списке `resultDetails`:
- Иконка `CheckCircle2` уже использует класс `text-success`. Проверить токен `--success` в `src/index.css` — если он не зелёный, заменить на явный зелёный (`text-green-500`) и фон `bg-green-500/10`, рамка `border-green-500/30`.
- Аналогично для выделения выбранного правильного варианта во время игры (опционально, оставим как есть, т.к. запрос про итоговый экран).

### Технические детали
- Импорт: добавить `Gift` из `lucide-react`, убрать неиспользуемые `RotateCcw`, `useNavigate` (если больше не нужны).
- Удалить переменную `level` если она больше не показывается (оставим — это бейдж уровня).
- Сабмит попытки в БД (`submit_quiz_attempt`) уже происходит — данные для определения победителей сохраняются в `quiz_attempts` (score == total_questions).